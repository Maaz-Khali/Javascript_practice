const notes=[{
title:'my book',
body:'meri kitab'},
{
    title:'my house',
    body:'mera ghar'},
    {
        title:'my family',
        body:'meri family'}
]


 
console.log(notes)

/*const index= notes.findIndex( function(note, index){
    
    return note.title === 'my family'
})
console.log(index)
*/
/*   console.log(notes.pop())
    notes.push('mera note')
    console.log(notes.shift())
    notes.unshift('pehla program')
    */
/*notes.forEach(function (item, index){
    console.log(index)

    console.log(item)
})
console.log(notes)
for(let count=0; count<=2; count++){
    
    console.log(count);
}
for(let count=0; count<notes.length; count++){
    console.log(notes[count]);
}*/